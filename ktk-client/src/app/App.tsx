import type React from "react";
import { Button } from "@/shared/components/ui/button";

const App: React.FC = () => {
    return (
        <div className="wrap">
            <Button>Test text</Button>
        </div>
    );
};

export default App;
